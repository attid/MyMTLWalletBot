# === Bot Identity ===
# Telegram Bot Token for production
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Telegram Bot Token for testing/development
TEST_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Environment: 'production' or 'test'. Defaults to 'test'.
ENVIRONMENT=test

# === Database & Redis ===
# Firebird Database URL (e.g., driver://user:password@host///path/to/db.fdb)
DB_URL=firebird+fdb://SYSDBA:sysdba@127.0.0.1///db/mmwb.fdb
# MongoDB URL for document storage
MONGODB_URL=mongodb+srv://user:pass@cluster.mongodb.net/
# Redis URL for caching and messaging
REDIS_URL=redis://localhost:6379/0

# === Stellar Blockchain ===
# base_fee is in stroops (1 XLM = 10,000,000 stroops). 100 stroops = 0.00001 XLM.
BASE_FEE=100
# Horizon API URL (Public)
HORIZON_URL=https://horizon.stellar.org/
# Horizon API URL (Read-Write/Private/Dedicated)
HORIZON_URL_RW=https://horizon.stellar.org

# === Tron Blockchain ===
TRON_API_KEY=your_tron_api_key
TRON_MASTER_ADDRESS=your_tron_master_address
TRON_MASTER_KEY=your_tron_master_private_key

# === TON Blockchain ===
TONCONSOLE_TOKEN=your_tonconsole_token
TON_TOKEN=your_ton_token
TONCENTER_TOKEN=your_toncenter_token

# === Integrations ===
# OpenAI API Key for AI features
OPENAI_KEY=sk-your-openai-key
# ThothPay API Key
THOTHPAY_API=your_thothpay_key
# EURMTL Key (if applicable)
EURMTL_KEY=your_eurmtl_key
# Grist Token (for spreadsheets integration)
GRIST_TOKEN=your_grist_token
# DeBank API Key (Optional)
DEBANK=your_debank_key
# Sentry DSN for error tracking. If shorter than 20 chars, Sentry will be disabled.
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# === Bot Configuration ===
# Cost to create a wallet (in native currency units?)
WALLET_COST=1.0
# List of Admin IDs (comma separated if supported, currently parsed in code as list)
ADMIN_LIST=12345678,87654321

# === Notification Service ===
# URL of the Operations Notifier service (internal or external)
NOTIFIER_URL=http://operations-notifier:8000
# Public URL where THIS bot receives webhooks (accessible by Notifier)
WEBHOOK_PUBLIC_URL=http://mmwb_bot:8081/webhook
# Port to listen for webhooks internally
WEBHOOK_PORT=8081

# [Security] Operations Notifier Public Key (to verify incoming webhooks)
# Leave empty to disable signature verification (not recommended for public access).
# Example: G...
NOTIFIER_PUBLIC_KEY=G...

# [Security] Service Secret Key (to sign outgoing requests to Notifier)
# Must correspond to a Public Key authorized in the Notifier's config.
# Example: S...
SERVICE_SECRET=S...

# [Security] Auth Token (alternative to Service Secret, for Notifier >= 0.5.3)
# If set, this token starts with "Authorization: " header or is sent as is?
# Actually, code sends it as `Authorization: <token>`, so just put the token here.
# Example: my-secure-token
NOTIFIER_AUTH_TOKEN=

# === WebApp ===
# URL of the WebApp for biometric signing (must be HTTPS in production)
WEBAPP_URL=https://webapp.example.com