# ADR Index

Architecture Decision Records capture non-trivial technical decisions.

Rules:

- one file per decision,
- immutable history (supersede with a new ADR, do not rewrite old decisions),
- include context, decision, consequences.

Use `adr/template.md` as the starting point.
